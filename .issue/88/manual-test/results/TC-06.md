# TC-06: 入力中の `/` はショートカットに奪われない

結果: PASS

## 手順と確認
1. `#sidebar-search` に focus し、`a/b` を type
2. eval `document.getElementById('sidebar-search').value` → `a/b`
   - activeElement.id も `sidebar-search` のまま
   - 入力フォーカス中の `/` はショートカットに奪われず文字として入力されることを確認

## console
エラーなし
